% =====================================================================
%  Método 11 — Embeddings   (dimensão: semântica)
%  Origem: células 49, 50 e 51 do caderno
% =====================================================================
\subsection{Embeddings}
\label{met:embeddings}

Todos os métodos anteriores tratam a palavra como símbolo: ela pertence ou não ao léxico, casa
ou não com a regra, possui ou não contagem no corpus, e duas palavras distintas são
simplesmente distintas, sem gradação. Este método substitui o símbolo por um ponto no espaço,
obtendo com isso uma noção de proximidade ausente nos demais.

O fundamento é a hipótese distribucional, segundo a qual palavras que ocorrem nos mesmos
contextos possuem significados relacionados~\cite{mikolov2013}. O vetor é construído de modo a
sumarizar os contextos de ocorrência da palavra no corpus de treino, e o resultado é uma
tabela $\mathbf{v} : \mathcal{V} \to \mathbb{R}^{d}$ com uma linha por palavra, aqui com
$d = 300$ e 20.000 linhas. Trata-se de consulta, e não de cálculo, uma vez que o vetor de cada
palavra é invariante e está armazenado no modelo. Desta propriedade decorrem tanto os acertos
quanto os limites do método: o vetor não codifica o significado da palavra, mas os contextos
em que ela habitualmente ocorre.

A comparação é feita pelo cosseno, isto é, pelo ângulo entre os dois vetores:
%
\begin{equation}
\cos(\mathbf{a}, \mathbf{b})
= \frac{\mathbf{a} \cdot \mathbf{b}}{\lVert \mathbf{a}\rVert \, \lVert \mathbf{b}\rVert}
\end{equation}
%
O valor situa-se entre $-1$ e $+1$, assumindo $1$ quando os vetores apontam na mesma direção e
$0$ quando são ortogonais. Emprega-se o cosseno, e não a distância euclidiana, porque a norma
$\lVert \mathbf{v} \rVert$ veicula sobretudo frequência, dado que palavras comuns tendem a
vetores de maior comprimento; a divisão pelas duas normas descarta essa magnitude e retém
apenas a direção, onde reside a informação contextual.

A extensão mais simples da medida para a frase é a média dos vetores de suas palavras:
%
\begin{equation}
\vetor{\sent} = \frac{1}{|\sent|} \sum_{t \in \sent} \vetor{t}
\end{equation}
%
O somatório percorre um multiconjunto, de modo que qualquer reordenação da frase apresenta
exatamente os mesmos termos e, por consequência, a mesma soma. A Seção~\ref{sec:empiricos}
examina o que daí decorre.

O método devolve um número entre $-1$ e $+1$ por par de palavras, conforme a
Tabela~\ref{tab:embeddings}.

\begin{table}[H]
\centering
\dimtable{
\begin{tabular}{@{}llr@{}}
\toprule
\textbf{Par} & \textbf{Relação} & $\cos$ \\
\midrule
\texttt{seco} × \texttt{úmido}      & antônimos   & $+0{,}838$ \\
\texttt{quente} × \texttt{frio}     & antônimos   & $+0{,}690$ \\
\texttt{área} × \texttt{região}     & sinônimos   & $+0{,}658$ \\
\texttt{chamas} × \texttt{fogo}     & sinônimos   & $+0{,}648$ \\
\texttt{mata} × \texttt{floresta}   & sinônimos   & $+0{,}610$ \\
\texttt{início} × \texttt{fim}      & antônimos   & $+0{,}562$ \\
\texttt{incêndio} × \texttt{fogo}   & sinônimos   & $+0{,}509$ \\
\texttt{ativo} × \texttt{extinto}   & antônimos   & $+0{,}263$ \\
\midrule
\texttt{fogo} × \texttt{sintaxe}    & sem relação & $+0{,}065$ \\
\texttt{incêndio} × \texttt{gramática} & sem relação & $+0{,}005$ \\
\texttt{área} × \texttt{advérbio}   & sem relação & $-0{,}063$ \\
\bottomrule
\end{tabular}}
\caption{Cossenos entre pares de palavras do domínio, ordenados por proximidade.}
\label{tab:embeddings}
\end{table}

O bloco inferior estabelece a referência de leitura, uma vez que o zero corresponde à ausência
de relação e é contra ele que os demais valores devem ser interpretados. A ordenação do bloco
superior constitui o resultado do método sobre o corpus, e a Seção~\ref{sec:empiricos} a
discute~\cite{faruqui2016,schnabel2015}.
